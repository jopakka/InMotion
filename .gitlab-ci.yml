stages:
    - build

say_hello:
    stage: build
    script:
        - echo Hello
    tags:
        - iOS

build_project:
    stage: build
    script:
        - xcodebuild clean -project InMotion.xcodeproj -scheme InMotion #| xcpretty
        - xcodebuild test -project InMotion.xcodeproj -scheme InMotion -destination 'platform=iOS Simulator,name=iPhone XR,OS=12.1' #| xcpretty -s
    tags: iOS
